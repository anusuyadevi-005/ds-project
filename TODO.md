# TODO - AQI Website

- [ ] Create initial website folder structure (`site/`, `site/assets/`, `site/data/`).
- [x] Create data preparation output file needed by the frontend (`site/data/stations_geo.csv`).

- [ ] Implement frontend UI (`index.html`) with city + pollutant selectors.
- [ ] Implement station map with Leaflet + marker clustering (optional) using `stations_geo.csv`.
- [ ] Implement charts with Plotly (pollutant_avg summary and station-level view).
- [ ] Add local run instructions (static server) so it works in browser.
- [ ] Validate by opening the page locally and checking that filters update visuals.

